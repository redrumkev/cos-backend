##############################################
# CI / Cursor dummy DB credentials – safe to commit
##############################################
POSTGRES_USER=cos_user
POSTGRES_PASSWORD_DEV=cos_dev_pass

POSTGRES_DEV_URL=postgresql+asyncpg://cos_user:cos_dev_pass@localhost:5433/cos_db_dev
POSTGRES_MIGRATE_URL=postgresql+psycopg://cos_user:cos_dev_pass@localhost:5433/cos_db_dev

POSTGRES_DB_DEV=cos_db_dev
POSTGRES_DB_PROD=cos_db_prod

MEM0_SCHEMA=mem0_cc

ENV=development
LOG_LEVEL=debug
PROJECT_NAME=cos
